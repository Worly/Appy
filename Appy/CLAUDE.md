# CLAUDE.md — Backend (Appy/)

ASP.NET Core 8 Web API. Serves the REST API and, in production, also serves the Angular frontend as static files.

## Child CLAUDE.md Files

**Read the relevant CLAUDE.md before editing files in any of these folders:**

| Folder | CLAUDE.md |
|--------|-----------|
| `Domain/` | `Domain/CLAUDE.md` |
| `Controllers/` | `Controllers/CLAUDE.md` |
| `Services/` | `Services/CLAUDE.md` |
| `Services/MessagingServices/` | `Services/MessagingServices/CLAUDE.md` |
| `Services/SmartFilter/` | `Services/SmartFilter/CLAUDE.md` |
| `Services/Facilities/` | `Services/Facilities/CLAUDE.md` |
| `DTOs/` | `DTOs/CLAUDE.md` |
| `Auth/` | `Auth/CLAUDE.md` |
| `Exceptions/` | `Exceptions/CLAUDE.md` |
| `Utils/` | `Utils/CLAUDE.md` |

## Request Pipeline (order matters)

```
Request
  → ExceptionMiddleware       (catches HttpException → JSON response)
  → JwtMiddleware             (validates Bearer token → attaches User to HttpContext.Items)
  → FacilityMiddleware        (reads facility-id header → stores in HttpContext.Items)
  → [AuthorizeAttribute]      (action filter: 401 if no user)
  → [SelectedFacilityAttribute] (action filter: validates ownership, 400/404)
  → Controller Action
```

## Dependency Injection (Program.cs)

All application services are registered as **Scoped**. The one exception is `CronScheduler` (Singleton), which runs `AppointmentReminderService` on a 5-minute interval.

## Configuration

- **Development**: `appsettings.json`
- **Production**: environment variables (UPPER_SNAKE_CASE — see `Utils/CLAUDE.md`)
- `NO_FRONTEND=true` env var skips static file serving (used in CI)

## Database

PostgreSQL via `Npgsql.EntityFrameworkCore.PostgreSQL`. Single `AppDbContext`. Migrations are in `Migrations/` (auto-generated by EF Core — do not hand-edit). Apply with `dotnet ef database update`.

## Health Check

`GET /health` returns 200 OK. Used by CI/CD to wait for the backend to be ready before running E2E tests.

## Static File Serving (Production)

When not in development and `NO_FRONTEND` is not set, the backend serves the Angular build from `Appy-frontend/dist/` and falls back all unmatched routes to `index.html` (for Angular's client-side routing).
